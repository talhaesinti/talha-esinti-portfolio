/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['github.com', 'your-domain.com'], // Kullanacağınız image domainleri
  },
};

module.exports = nextConfig; 